name: Deploy to DigitalOcean Droplet
on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Deploy to DigitalOcean Droplet
        uses: actions/checkout@v2
        with:
          host: 188.166.165.242
          username: root
          key: Welcome@023@W2a
          script: |
            export NVM_DIR=~/.nvm
            source ~/.nvm/nvm.sh
            rm -rf test
            mkdir test
            cd test
            git clone git@github.com:goheersajjadtradata/python-project.git
            docker-compose -f docker-compose.yml build
            docker-compose -f docker-compose.yml up -d
            echo 'Deployment to digital ocean finished'
